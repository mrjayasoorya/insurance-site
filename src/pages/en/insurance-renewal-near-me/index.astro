---
// RESTRUCTURED: Added Breadcrumbs, howWeWork, dual CTA, removed LocalIdentitySection

import DesktopHero from "../../../components/DesktopHero.astro";
import MobileHero from "../../../components/MobileHero.astro";
import MobileAccordion from "../../../components/MobileAccordion.astro";
import Breadcrumbs from "src/components/Breadcrumbs.astro";
import BacklinksRow from "../../../components/BacklinksRow.astro";
import FooterCtaBar from "../../../components/FooterCtaBar.astro";
import GlobalOverlays from "src/components/GlobalOverlays.astro";
import PageActionBar from "src/components/PageActionBar.astro";

import { siteData, EN } from "../../../content/siteData";
import { canonicalFromPath, buildMeta, buildJsonLd, geoMetaFromBrand } from "../../../lib/seo";
import ConversionBlock from "src/components/ConversionBlock.astro";
import ScenariosWeHandle from "src/components/ScenariosWeHandle.astro";
import ThinkingGapFiller from "src/components/ThinkingGapFiller.astro";
import WhyChooseUs from "src/components/WhyChooseUs.astro";
import UserQuestionsBlock from "src/components/UserQuestionsBlock.astro";
import SmartAssistForm from "src/components/SmartAssistForm.astro";

const page = siteData.intentPages["insurance-renewal-near-me"];
const cc = siteData.confidenceContent || {};
const waNumber = (siteData.brand.whatsappE164 || "").replace("+", "");
const canonical = canonicalFromPath(siteData.brand.websiteUrl, "/en/insurance-renewal-near-me/");
const meta = buildMeta({ title: `${page.seoTitle} | ${siteData.brand.shortName}`, description: page.seoDesc, canonical, geo: geoMetaFromBrand(siteData) });

const jsonLd = buildJsonLd({
  pageType: "intent",
  canonical,
  siteData,
  title: meta.title,
  description: meta.description,
  breadcrumbs: [
    { name: "Home", href: canonicalFromPath(siteData.brand.websiteUrl, "/en/") },
    { name: "Insurance Renewal Near Me", href: canonical },
  ],
  serviceData: page,
  faqs: page.faqs || [],
});
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{meta.title}</title>
    <link rel="icon" type="image/png" href="/assets/logo/MNRajendraKumar_Insurance_services_logo_48x48px_white_bg_icon.png"/>
    <meta name="description" content={meta.description} />
    <meta name="robots" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1" />
    <meta name="googlebot" content="index,follow,max-image-preview:large,max-snippet:-1,max-video-preview:-1" />
    <link rel="canonical" href={meta.canonical} />
    <link rel="stylesheet" href="/styles/theme.css" />
    {Object.entries(meta.tags).map(([k, v]) => <meta property={k} content={v} />)}
    {Object.entries(meta.nameTags || {}).map(([k, v]) => <meta name={k} content={v} />)}
    <script type="application/ld+json" set:html={JSON.stringify(jsonLd)} />
  </head>

  <body>
    <main class="app">
      <div class="container">
        <!-- ========================= DESKTOP ========================= -->
                <!-- SINGLE DOM: merged; divergent blocks remain per-breakpoint via ui-desktop/ui-mobile -->
        <div class="ui-desktop">
          <header>
            <DesktopHero brand={{ name: siteData.brand.name, tagline: siteData.brand.tagline, subtitle: page.heroP, phone: siteData.brand.phone, areaChips: siteData.brand.areaChips }} />
          </header>

          <Breadcrumbs items={[
            { label: "Home", href: EN("/") },
            { label: page.heroH1 },
          ]} />
        </div>

        <div class="ui-mobile">
          <header>
            <MobileHero brand={{ name: siteData.brand.name, tagline: siteData.brand.tagline, subtitle: page.heroP, phone: siteData.brand.phone, areaChips: [] }} />
          </header>
        </div>

<!-- 1. H1 + CTA -->
          <section class="section section-padding">
            <h1 class="section-title">{page.heroH1}</h1>
            <p class="section-sub readable">{page.heroP}</p>
            <PageActionBar brand={siteData.brand} context={page.heroH1 || ""} />
          </section>

<!-- 2. Renewal checks -->
          <section class="section section-padding">
            <h2 class="section-title">{page.renewalChecks.title}</h2>
            <ul class="bullets">{page.renewalChecks.bullets.map((x) => <li>{x}</li>)}</ul>
          </section>

<!-- USER QUESTIONS: area, language, scope -->
          <UserQuestionsBlock
            title="Common questions before you contact us"
            items={cc.userQuestions || []}
          />

<!-- SMART ASSIST — renewal capture -->
          <SmartAssistForm
            brand={siteData.brand}
            id="sa-desk"
            heading="Renewal coming up? Tell us what you have"
            subheading="Pick your policy type — we pre-fill the WhatsApp message, you just send."
          />

<!-- SCENARIOS WE HANDLE -->
          <ScenariosWeHandle
            title="Real renewal situations we handle"
            subtitle="Expired policies, lapsed coverage, NCB queries — these are common. We've seen it all."
            scenarios={(cc.homeScenarios || []).slice(0, 3)}
            waNumber={waNumber}
          />

<!-- STILL WONDERING -->
          <ThinkingGapFiller
            title="Still have renewal doubts?"
            items={(cc.homeGapQuestions || []).slice(0, 4)}
            waNumber={waNumber}
            waText="Hi, I need help renewing my insurance. Can you guide me?"
          />

<!-- WHY CHOOSE US -->
          <WhyChooseUs
            title="Why work with us for renewals"
            points={(cc.whyUs || []).slice(0, 4)}
          />

<div class="ui-desktop">
<!-- 4. FAQs -->
          {page.faqs?.length ? (
            <section class="section section-padding">
              <h2 class="section-title">Frequently asked questions</h2>
              <MobileAccordion sections={[{ title: "Questions people ask", items: page.faqs.map((f) => ({ strong: f.q, text: f.a })) }]} defaultOpen={true} />
            </section>
          ) : null}
        </div>

<div class="ui-mobile">
<!-- 3. FAQs -->
          {page.faqs?.length ? (
            <section class="section section-padding">
              <MobileAccordion sections={[{ title: "FAQs", items: page.faqs.map((f) => ({ strong: f.q, text: f.a })) }]} defaultOpen={true} />
            </section>
          ) : null}
        </div>

<ConversionBlock siteData={siteData} brand={siteData.brand} />

<BacklinksRow title={siteData.backlinks.title} links={siteData.backlinks.links} />

<FooterCtaBar {...{
            title: siteData.footerCtas.title,
            subtitle: siteData.footerCtas.subtitle,
            whatsappNumber: siteData.brand.whatsappE164,
            whatsappText: siteData.brand.whatsappPrefill,
            callNumber: siteData.brand.phoneE164,
            callLabel: siteData.footerCtas.callLabel,
            brandName: siteData.footerCtas.brandName,
            logoText: siteData.footerCtas.logoText,
            copyright: siteData.footerCtas.copyright,
          }} />
      </div>
    </main>

    <GlobalOverlays />

    <style is:global>
      .readable{max-width:78ch}
      .grid{display:grid;gap:12px}
      .grid-2{grid-template-columns:repeat(2,minmax(0,1fr))}
      .card{display:block;padding:14px;border:1px solid rgba(255,255,255,.12);border-radius:14px;text-decoration:none}
      .card-title{font-size:16px;margin:8px 0 6px}
      .card-desc{margin:0;opacity:.85}
      @media (max-width: 980px){.grid-2{grid-template-columns:1fr}}
    </style>
  
    <style is:global>
      /* Single-DOM merge: hide desktop-only extras below the existing 820px switch */
      @media (max-width: 820px){
        .swh > .section-sub{display:none}
        .swh-grid > .swh-card:nth-child(n+3){display:none}
        .tgf > .section-sub{display:none}
        .tgf-list > .tgf-item:nth-child(n+4){display:none}
      }
    </style>
  </body>
</html>
